// Show the Modal
// const [visible, setVisible] = React.useState(false);

// const showModal = () => setVisible(true);
// const hideModal = () => setVisible(false);
// const containerStyle = { backgroundColor: 'white', padding: 20 };
{/* <StyledModal
    className='flex justify-center items-center bg-blue-500'
    style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 22,
    }}
    animationType="slide"
    transparent={true}
    visible={visible}
    onRequestClose={() => {
        Alert.alert('Modal has been closed.');
        setVisible(!visible);
    }}>
    <View className='flex justify-center items-center h-screen bg-gray-900 opacity-90'>
        <View className='bg-white rounded shadow shadow-black p-4' style={{ height: '50%', width: '80%' }}>
            <StyledView className='flex flex-row justify-between'>
                <Text className='text-base' style={{ fontFamily: 'Poppins_700Bold' }}>Pilih Kelas</Text>
                <Pressable
                    onPress={() => setVisible(!visible)}>
                    <Image source={close} className='h-6 w-6' />
                </Pressable>
            </StyledView>
        </View>
    </View>
</StyledModal> */}